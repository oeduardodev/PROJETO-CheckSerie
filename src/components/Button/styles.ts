import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    marginTop: 14,
    backgroundColor: "#12699bff",
    width: "100%",
    height: 48,
    borderRadius: 6,
    justifyContent: "center",
    alignItems: "center",
  },
  buttonText: {
    color: "#eeeeeeff",
    fontSize: 14,
    fontWeight: "bold",
  },
});
